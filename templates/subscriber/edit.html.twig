{% extends 'base.html.twig' %}

{% block title %}Edit Subscriber{% endblock %}

{% block body %}
    <h1>Edit Subscriber</h1>

    {{ include('subscriber/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('admin_subscriber_index') }}" class="btn btn-secondary">back to list</a>

    {{ include('subscriber/_delete_form.html.twig') }}
{% endblock %}
